# نظام مواقف سيارات الأكاديمية الوطنية للبناء

## 🔧 **إصلاح مشاكل RLS (Row Level Security)**

### المشكلة:
كانت هناك مشكلة في سياسات الأمان (RLS) في Supabase تمنع المستخدمين من إضافة/تعديل/حذف السيارات.

### الحل:
تم استخدام سياسات مبسطة تسمح بالوصول الكامل للجميع للتجربة:

```sql
CREATE POLICY "الوصول الكامل للجميع" ON cars
FOR ALL USING (true);